// ===========================================
// Wishlist Controller
// ===========================================
// HTTP routes for wishlist: GET, POST, PUT, DELETE /wishlist

import { Controller } from '@nestjs/common';

@Controller('wishlist')
export class WishlistsController {
  // constructor(private readonly service: WishlistsService) {}
  // @Get() findAll() {}
  // @Get(':id') findById() {}
  // @Post() create() {}
  // @Put(':id') update() {}
  // @Delete(':id') remove() {}
}
