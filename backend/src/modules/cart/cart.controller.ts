// ===========================================
// Cart Controller
// ===========================================
// HTTP routes for cart: GET, POST, PUT, DELETE /cart

import { Controller } from '@nestjs/common';

@Controller('cart')
export class CartsController {
  // constructor(private readonly service: CartsService) {}
  // @Get() findAll() {}
  // @Get(':id') findById() {}
  // @Post() create() {}
  // @Put(':id') update() {}
  // @Delete(':id') remove() {}
}
