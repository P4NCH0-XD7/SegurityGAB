// ===========================================
// SaleDetail Service
// ===========================================
// Business logic for sale-details management.

import { Injectable } from '@nestjs/common';

@Injectable()
export class SaleDetailsService {
  // constructor(private readonly repository: SaleDetailsRepository) {}
  // async findAll() {}
  // async findById(id: number) {}
  // async create(dto: CreateSaleDetailDto) {}
  // async update(id: number, dto: UpdateSaleDetailDto) {}
  // async remove(id: number) {}
}
